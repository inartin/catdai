export const metadata = {
  title: "Paddle payment status | Catdai",
  robots: {
    index: false,
    follow: false,
  },
};

export default function PaddleSuccessLayout({ children }) {
  return children;
}
