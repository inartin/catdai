export const metadata = {
  title: "Paddle checkout | Catdai",
  robots: {
    index: false,
    follow: false,
  },
};

export default function PaddleCheckoutLayout({ children }) {
  return children;
}
